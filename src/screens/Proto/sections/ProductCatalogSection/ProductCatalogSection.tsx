import React from "react";

export const ProductCatalogSection = (): JSX.Element => {
  return (
    <div className="mt-[90px]">
      {/* This section now only contains the catalog grid, text moved to main component */}
    </div>
  );
};